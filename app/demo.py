#
# from sqlalchemy import create_engine, inspect
# from app.database import DATABASE_URL
# eng = create_engine(DATABASE_URL)
# insp = inspect(eng)
# print(insp.get_columns('files'))

# from sqlalchemy import create_engine, inspect
# from app.database import DATABASE_URL
# eng = create_engine(DATABASE_URL)
# insp = inspect(eng)
# print(insp.get_columns('files'))
